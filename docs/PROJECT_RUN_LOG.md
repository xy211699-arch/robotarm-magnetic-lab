# 项目运行日志

> 起始日期：2026-07-27  
> 维护规则：从本轮对话开始，每轮完成工作后追加一条；只记录请求、实际完成、
> 验证结果、遗留问题和下一步，不重复长篇背景。

## 2026-07-27｜建立增量总结与持续日志

- 请求：总结 2026-07-17 交接报告之后的工作，并建立每轮更新的项目日志。
- 完成：生成 `POST_HANDOVER_WORK_SUMMARY.md`，覆盖 Isaac Lab 迁移、磁力/胶囊物理、
  相机、验证、微调数据接口和胃部资产预审。
- 结论：训练平台基础与数据管线已可用；胃部资产接入、任务奖励、专家数据和软组织
  仿真仍待完成。
- 下一步：清洗 `stomach_physics.usd`，生成 Z-up 独立胃部训练资产并接入单环境。

## 2026-07-28｜整理 Isaac Lab 现有成果验收流程

- 请求：给出在 Isaac Lab 中验证已有工作的代码指令。
- 完成：核对任务注册、GUI 冒烟、九轴接口、磁力/重力/接触、数据采集与数据集校验
  脚本均存在，并整理成可依次执行的验收命令和通过标志。
- 验证：`list_envs.py`、`zero_agent.py`、`validate_interfaces.py`、
  `test_magnetic_tilt.py`、`collect_finetune_dataset.py`、`validate_dataset.py` 和
  `build_finetune_index.py` 均已找到。
- 下一步：执行完整验收；全部通过后再接入并清洗胃部 USD 资产。

## 2026-07-28｜修复任务注册检查的启动参数

- 请求：处理 `list_envs.py` 不识别启动器自动注入的 `--kit_args`。
- 原因：项目启动脚本统一追加 UJITSO Kit 参数，但环境列表脚本未注册
  `AppLauncher` 命令行参数。
- 完成：让 `list_envs.py` 使用标准 `AppLauncher` 参数解析并保持无界面启动。
- 验证：原命令退出码为 0，成功列出 `Template-Robotarm-Magnetic-Lab-v0`。
- 下一步：重新执行任务注册检查，然后继续九轴接口与磁力接触验收。

## 2026-07-28｜解释微调数据集与时序训练索引

- 请求：在前四项验收通过后，说明 Isaac Lab 中微调数据集和时序索引的含义。
- 核查：现有 `robotarm_magnetic_v1` 含 1 个成功 episode、12 个控制步和 2 个
  `history=4/horizon=8` 时序样本。
- 结论：现有数据仅验证 RGB-D、31维状态、9维动作和仿真特权标签的同步记录管线；
  示教主要激励球关节，不足以训练最终六轴机械臂加球关节策略。
- 下一步：先验证数据文件，再设计覆盖胃内任务的专家策略、随机化和大规模采集方案。

## 2026-07-28｜重新审查并接入正确胃部资产

- 请求：撤销错误的 `stomach_physics.usd` 认定，改用
  `stomach_capsule_test_v1.usd` 接入Isaac Lab。
- 检查：源资产为Y-up、米制，含49,047面视觉/静态碰撞网格、4096² RGBA纹理、
  25×13 mm测试胶囊、重复PhysicsScene和测试刚体；源重力字段无效，墙面静摩擦
  小于动摩擦。
- 完成：复制并打包源USD/纹理，新增Z-up清洗包装层，禁用重复PhysicsScene、
  测试胶囊、规划重复网格和台面标志物；保留内向静态三角网格碰撞与双面内壁渲染。
- 完成：新增独立任务 `Template-Robotarm-Magnetic-Stomach-Lab-v0`，原台面任务保留
  作为回归基准。
- 修正：禁用真实台面Prim `/MagneticDemo/ground`；将Y-up到Z-up变换下移到子Prim，
  避免被Isaac Lab根姿态覆盖；按实测胃底静止点重新对齐复位位置。
- 验证：新任务注册成功；20步零动作测试完成，胶囊从
  `[1.060815, 0.114537, 0.006500]`稳定到
  `[1.060674, 0.114698, 0.006626]`，未再自由落体；RGB可见胃壁纹理，深度最大约
  `0.124 m`，九轴、磁力桥和ASM间距仍正常。
- 拓扑：视觉网格有49,047面、21条边界边、0条非流形边，属于带开口而非完全封闭
  曲面；训练前需明确贲门/幽门开口的物理边界策略。
- 限制：当前胃壁仍是刚性静态网格；湿组织摩擦、柔顺/形变和胃壁运动尚未标定。
- 下一步：增加胃壁接触传感/穿透验收，降低内镜过曝并校准湿组织摩擦，再进行
  Ball磁驱动胃内运动测试。

## 2026-07-28｜核算胃部模型实际尺寸

- 请求：确认当前Isaac Lab胃部模型大小。
- 验证：按Z-up清洗层和当前对齐变换计算视觉网格世界轴对齐包围盒。
- 结果：X/Y/Z尺寸为 `67.749 × 48.579 × 147.711 mm`；仅位置和坐标轴发生变换，
  未对源模型缩放。

## 2026-07-28｜胃部2倍等比放大并调整为Y向

- 请求：胃部等比例放大2倍，调整为Y-up，其余系统条件不变。
- 处理：保持Isaac Lab Stage为Z-up和重力−Z，取消胃部Y→Z旋转，使胃部自身长轴沿
  世界+Y；视觉、碰撞和纹理统一缩放2倍。
- 尺寸：最终X/Y/Z包围盒为 `135.498 × 295.422 × 97.158 mm`。
- 校准：通过两轮100步零动作测试迭代重力、磁力与曲面接触联合平衡点；最终胶囊从
  `[1.060815, 0.114537, 0.006500]`稳定到
  `[1.060870, 0.114885, 0.006510]`，位移约0.352 mm。
- 验证：九轴、磁力桥、相机RGB-D与碰撞继续运行；胃壁纹理可见，无自由落体或穿壁。
- 遗留：内镜图像仍能看到胃部开口/背景区域，后续需配置开口边界和曝光。

## 2026-07-28｜胃部改为1.8倍并上下翻转

- 请求：将2倍缩放改为1.8倍，并按当前世界坐标上下翻转胃部。
- 处理：胃部统一缩放改为1.8；绕世界Y轴旋转180°，保持长轴沿Y且反转世界Z上下；
  Stage、重力、机器人、磁场、胶囊和控制接口不变。
- 尺寸：最终X/Y/Z包围盒为 `121.948 × 265.880 × 87.442 mm`。
- 校准：用翻转后源上壁交点初始化，并进行两次100步重力、磁力、接触联合平衡回代。
- 验证：胶囊100步由 `[1.060815, 0.114537, 0.006500]`到
  `[1.061229, 0.115126, 0.006558]`，总漂移约0.722 mm；无穿壁、坠落或求解发散，
  RGB-D与九轴磁力系统正常。

## 2026-07-28｜增加可选胶囊相机实时窗口

- 请求：在Isaac Lab主视口外实时显示胶囊相机图像，并通过启动参数控制。
- 完成：新增 `--capsule_camera_view`；零动作、九轴验收、磁驱动测试和数据采集脚本
  均可创建独立Kit图像窗口，未开启时无额外图像处理开销。
- 数据：窗口显示模型实际使用的1280×720圆形广角RGB，而非原始矩形RTX缓冲；刷新
  挂接30 Hz渲染节拍，保持20 Hz策略频率不变。
- 验证：CLI帮助与参数兼容检查通过；当前受控执行环境无X Server/GPU，无法代替本机
  完成窗口像素验收，已提供本机启动命令和正常日志标志。
- 下一步：在RTX 5090图形会话中运行胃部任务，核对第二窗口图像、跟随和实际帧率。

## 2026-07-28｜修复实时窗口的相机传感器路径

- 现象：Kit窗口初始化时无法用 `{ENV_REGEX_NS}` 找到已注册的胶囊相机。
- 原因：场景构建后Camera注册路径已展开为 `/World/envs/env_.*/...`，可视化器不会再次
  解析场景配置占位符。
- 修复：显示窗口改用运行时Camera路径，面板绑定与运行后匹配逻辑同步更新。
- 下一步：重新启动胃部任务，确认出现 `CAPSULE_CAMERA_VIEW enabled` 日志与第二窗口。

## 2026-07-28｜制定胃部环境完善路线

- 请求：在胃部、胶囊和机械臂基本交互完成后确定下一阶段工作。
- 评估：当前胃壁是静态精确三角网格，摩擦为固定经验值；湿环境使用集中阻尼近似，
  尚缺胶囊—胃壁接触力、穿透、接触点及开口边界的系统验收。
- 建议：先完成碰撞拓扑与接触传感基线，再做实物摩擦/阻尼标定；随后完善内镜光学和
  域随机化，最后分阶段加入蠕动及局部/整体软组织形变。
- 下一步：新增胃壁接触验收脚本和参数配置，执行静置、跌落、斜坡、磁驱动贴壁与
  多初始点穿透压力测试。

## 2026-07-29｜胃部任务相机由30 Hz调整为1 Hz

- 请求：确认胃部摄像头是否为30 Hz；若是则改为1 Hz。
- 确认：共享基础相机原配置为 `update_period=1/30 s`。
- 修改：仅胃部任务覆盖为 `update_period=1.0 s`；台架回归任务继续保持30 Hz。
- 同步：实时相机窗口标题和日志改为读取任务实际频率；胃部窗口仅在新Camera帧产生时
  以1 Hz上传图像，主视口渲染仍保持30 Hz。
- 下一步：启动胃部任务，确认日志为 `sensor_hz=1.0 render_hz=30.0`。

## 2026-07-29｜审计实物标定参数

- 请求：识别仿真平台中人为设定且建议实物标定的参数。
- 高优先级：两块磁铁实际剩磁/磁化轴/安装偏差与力矩曲线；胶囊总质量、质心和惯量；
  湿胃壁摩擦、接触柔顺、流体线性/角阻力；相机鱼眼内参、帧率、安装位姿和LED响应。
- 中优先级：机械臂零位/TCP安装、关节延迟与跟踪动态、Ball三轴伺服响应；胃部真实尺寸、
  局部刚度、形变和蠕动。
- 区分：物理步长、磁场数值网格、碰撞安全裕量、速度保护上限和奖励权重属于数值/
  安全/训练设计，应做收敛或鲁棒性测试，不作为物理量拟合。
- 下一步：建立版本化标定参数表，依次完成磁场台架、胶囊称量、湿面斜坡/牵引、相机
  标定板与机器人阶跃响应实验。

## 2026-07-29｜建立并执行胃部接触物理验收套件

- 请求：为静置、不同高度跌落、斜面滑动、磁吸贴壁、沿壁滚动转向、贴壁改变Ball姿态
  和多胃内初始点建立独立可视化测试，并依据记录数据判断接触是否成功。
- 实现：为胶囊启用PhysX接触报告和Isaac Lab ContactSensor；新增
  `scripts/contact_physics/`共享框架、7个可视入口、JSONL遥测、PASS/FAIL摘要及说明。
- 数据：记录位姿/速度、Ball动作与角度、法向力、16子步历史峰值、接触/离地时间、
  动力学摩擦估计和磁力/力矩；无磁场景显式清空施加磁力。
- 结果：7项均PASS；静置支撑力0.05468 N，5/15/30 mm跌落峰值0.323–0.341 N且无穿壁，
  磁吸力约5.0 mN，滚转位移12.18 mm，5个初始点均稳定接触。
- 输出：汇总见 `scripts/contact_physics/TEST_REPORT.md`，原始结果位于
  `logs/contact_physics/<scenario>/<timestamp>/`。
- 限制：当前结论仅证明刚性胃网格的仿真一致性；摩擦、柔顺、阻尼和胃壁形变仍需
  实物标定，且需由用户运行GUI脚本完成最终目视验收。

## 2026-07-29｜胶囊相机1 Hz采集与30 Hz调试预览解耦

- 请求：保持实际胶囊相机1 Hz，同时让独立观察窗口以30 Hz显示。
- 实现：策略/数据集继续使用 `capsule_camera`（胃部任务1 Hz）；仅启用
  `--capsule_camera_view` 时创建同位姿、同光学参数的 `capsule_camera_preview`
  （30 Hz），圆形广角处理后送入Kit窗口。
- 隔离：预览Camera未加入ObservationCfg或数据采集接口，不改变训练时序、帧索引和
  真实1 Hz输入；关闭可视化选项时不创建预览传感器，也不产生额外渲染开销。
- 识别：窗口标题同时标注 `30 Hz (recorded 1 Hz)`；启动日志输出
  `preview_hz=30.0 recorded_sensor_hz=1.0 enters_observations=false`。
- 验证：胃部任务8步Kit烟雾测试通过；预览窗口正常创建，策略Observation仍仅包含原
  `vision.rgb/depth`，未出现额外预览字段。

## 2026-07-29｜修复接触测试红色标记遮挡胶囊

- 现象：任务三只能看到红色球体，无法观察真实胶囊。
- 原因：测试启动器默认开启ContactSensor调试显示；Isaac Lab默认红色接触标记半径
  20 mm，大于胶囊13 mm直径，视觉上将胶囊完全遮住。该球仅为无物理属性的显示标记，
  不是测量刚体或替代胶囊。
- 修复：七项测试默认关闭接触标记；可用 `--contact_debug` 显式开启缩小后的2 mm标记，
  接触传感、JSONL记录和PASS/FAIL判断不受影响。
- 说明：补充七项测试的磁力开关、Ball指令和物理流程；明确任务三为无磁力、无Ball动作
  的三区域重力滑动，任务五/六才通过三轴Ball绝对位置目标改变外部磁场。胶囊始终被动，
  只受磁力/磁矩、重力、接触摩擦与阻尼作用。

## 2026-07-29｜胃壁单层壳与胶囊相机穿模诊断

- 检查：胃部视觉/碰撞网格均为24,529顶点、49,047三角面、21条边界边的同形表面网格；
  当前没有内外壁或实体厚度，`doubleSided`仅控制双面显示。
- 关键原因：25 mm胶囊前端位于局部Z=12.5 mm，相机却位于Z=18.5 mm，镜头超出碰撞体
  约6 mm；近裁剪面另为2 mm。因此本体未穿墙时，相机已可能越过或裁掉胃壁。
- 物理现状：胃壁使用静态精确三角网格，胶囊为13×25 mm解析Capsule碰撞体，240 Hz；
  尚未显式配置CCD、contact offset或rest offset。既有局部接触测试通过不能排除高速、
  开口边缘或其他胃区的实际穿透。
- 建议顺序：先把镜头移回胶囊前端内侧并降低近裁剪距离；再增加CCD、毫米级接触裕量和
  全胃表面穿透监测；如需真实壁厚，派生新资产向外加厚并封闭开口边缘，保持内腔表面
  和既有接触坐标不变，不直接向内挤占胃腔。

## 2026-07-29｜修正相机并将胃部缩放调整为1.7倍

- 相机：光心由胶囊局部Z=18.5 mm移至11.5 mm，即位于25 mm胶囊前端面内1 mm；
  近裁剪距离由2 mm降为0.5 mm，解决本体未穿墙但镜头先越壁和近表面被裁掉的问题。
- 胃部：统一缩放由1.8改为1.7，尺寸约为
  `115.17 × 251.11 × 82.58 mm`；平移重算为
  `(1.3792906443, -0.3648581537, 0.3528071209)`，保持已验证下壁接触锚点
  `(1.0608155, 0.1145374, 0)`不变。
- 方案：新增 `docs/STOMACH_WALL_THICKENING_PLAN.md`；建议保留内腔、向组织外侧生成
  初始3 mm厚外壁，连接但不封死解剖开口，并配合接触裕量和有符号距离监测。
- 验证：USD组合尺寸与精确碰撞检查通过；胃部任务5步烟雾测试通过；1.7倍资产的无磁
  静置测试PASS，末态约 `(1.061550, 0.114583, 0.006546) m`，持续接触、速度稳定、
  磁力记录为零。结果见
  `logs/contact_physics/resting/20260729_204703/summary.json`。

## 2026-07-29｜澄清胃壁接触参考与胶囊最终位置

- 胃壁参考锚点为世界坐标 `(1.0608155, 0.1145374, 0.0) m`，表示既有测试区域附近的
  内表面参考点，不是额外物体或精确接触报告点。
- 胶囊静置复位中心为 `(1.0608155, 0.1145374, 0.0065) m`；Z方向6.5 mm来自胶囊半径。
- 1.7倍缩放重算平移仅保持上述表面锚点不动，其余胃壁点随锚点等比例收缩。
- 无磁静置末态 `(1.061550, 0.114583, 0.006546) m` 是胶囊刚体中心，不是胃壁接触点
  或相机位置；相对复位中心总偏移约0.737 mm。当前ContactSensor记录合力而非接触点，
  若需精确接触坐标应增加PhysX接触对或最近点查询。

## 2026-07-29｜澄清胃部缩放与缩放中心

- 胶囊未参与胃部缩放，初始中心仍为
  `(1.0608155, 0.1145374, 0.0065) m`；仅胃部包装USD设置为统一1.7倍。
- 同时重算胃部自身平移，是为了将缩放中心等效放在既有下壁参考点；否则保持旧平移、
  仅修改scale会让源USD围绕其内部原点收缩，使整个胃部在世界坐标中移动约厘米量级。
- 该处理是胃部的“缩放+重新放置”，不是胶囊与胃部联合缩放。若需求是严格围绕源USD
  原点缩放并接受胃部位置变化，应恢复旧translate，只保留1.7 scale。

## 2026-07-29｜建立仿真—3D打印尺寸基准

- 原始胃部源USD包围盒为 `67.749 × 147.711 × 48.579 mm`；当前Isaac Lab组合资产
  是其1.7倍，薄层内表面包围盒为 `115.173 × 251.109 × 82.584 mm`。
- 结论：直接以100%打印原始USD不会匹配仿真；应导出并烘焙组合资产变换，或明确对
  原始网格应用170%缩放。优先使用带单位的3MF，STL需显式按mm解释。
- 加厚后应保持内腔不变、向外生长；因此最终外包围盒会增大。仿真和打印必须共同引用
  同一厚壁派生网格，并通过打印件XYZ实测建立材料/设备收缩补偿。
- 文档：3D打印导出和验收流程已补充到 `docs/STOMACH_WALL_THICKENING_PLAN.md`。

## 2026-07-30｜论文磁控运动方法接入方案分析

- 阅读：分析 `paper-final.pdf` 的控制架构、偶极映射、四元数转子闭环、轮速分配、
  机械臂接触优先控制和胶囊演示；本轮未修改IsaacLab代码。
- 论文边界：完整闭环对象是外部球形转子/永磁体，不是胶囊；胶囊倾斜、姿态转换和
  纵轴滚动仅给出实验流程，没有胶囊姿态/位置反馈控制律或自主导航公式。
- 接入：建议新增开环模式管理、期望磁场轨迹、场到主磁铁姿态映射、连续四元数/三轴
  Ball逆解、速度限制和预规划机械臂参考模块；胶囊状态只用于仿真动力学与离线评估，
  不反馈到控制器。
- 关键限制：当前胶囊圆柱磁铁 `polarization_axis_local=[0,0,1]` 与胶囊长轴一致；
  理想磁矩 `m×B` 沿自身磁矩轴分量为零，不能可靠驱动绕长轴滚动。模式3实施前必须
  确认实物为径向充磁，或将磁化轴/磁铁安装改为相对长轴具有横向分量，并同步仿真。
- 现有接口：20 Hz九维绝对关节动作、Ball速度上限0.8 rad/s、240 Hz有限尺寸磁力模型
  可继续使用；论文1.5 rad/s是其转子实验限速，不能未经验证直接替换当前上限。

## 2026-07-30｜平整桌面三运动简化场景方案

- 方案：新增独立桌面任务，不改动既有胃部任务；场景仅保留固定平面、被动胶囊、
  机械臂/ASM外部磁铁、磁力桥、相机与记录接口，以先标定接触和磁控再迁回胃内。
- 已确认胶囊圆柱磁铁为轴向充磁，磁矩与胶囊长轴平行；可用磁矩
  `m×B` 实现倾角/方位控制和直立到侧卧的平滑姿态转换。
- 关键边界：轴向充磁不能由均匀旋转磁场直接产生绕胶囊长轴的磁矩。纵轴滚动应由
  机械臂改变外磁铁位置产生横向梯度力，再经桌面静摩擦形成被动滚动；Ball维持长轴
  方向。若只允许固定机械臂原地转Ball，则不能保证第三种运动。
- 控制流程：无磁静置标定→有限磁场/力映射→倾斜方位→平滑放倒→横向梯度滚动；
  规划10 Hz、动作插值20 Hz、物理240 Hz，全部采用开环磁场轨迹，胶囊状态仅记录评估。
- 验收：记录姿态误差、接触/离地、穿透、磁力磁矩和滚动滑移率；第三模式以
  `位移/(半径×滚角)`接近1、长轴偏差小且无跳起为成功依据。
## 2026-07-30 — 平桌三运动模式九步实现与验收

- 新增独立平桌任务、轴向磁场逆映射、倾角方位旋转、直立转侧卧和梯度力长轴滚动控制。
- 胶囊保持被动；Ball 控制场方向，机械臂横移产生滚动所需梯度力。
- 平桌专用参数定为摩擦 `0.55/0.48`、阻尼 `0.10/0.50`、无胃液角阻力、滚动磁力上限 `0.040 N`。
- 五项自动验收全部 PASS；长轴滚动位移 55.86 mm、滚转 6.38 rad、滑移率 0.258、接触率 100%。
- 新增 `docs/TABLE_MOTION_ACCEPTANCE.md`，记录九项交付、运行指令、指标与实物标定边界。

## 2026-07-30 — 说明五个平桌可视化验收场景

- 明确测试01验证无磁静置、跌落接触和摩擦减速；测试02仅扫描Ball磁场方向，不驱动胶囊。
- 明确测试03验证固定倾角下方位旋转，测试04验证直立到侧卧的平滑姿态转换。
- 明确测试05验证机械臂梯度力驱动的长轴滚动，红色标记用于区分滚动与纯滑动。
- 补充各场景正常画面及穿透、弹飞、姿态突变、纯滑动等失败判据。

## 2026-07-31 — 连续复杂运动组合实现与验收

- 新增测试06：同一被动胶囊连续完成侧躺→直立→30°倾斜方位整圈→直立→
  侧躺→沿世界+X滚动100 mm，阶段间不重置。
- 修复Ball逆解单半球限制：组合任务临时采用`±π rad`动作范围，有限磁场逆解
  最大方向误差0.041°，胶囊方位累计旋转351.81°。
- 修复滚动段ASM碰撞：锁定避碰自由度j6，仅由j1..j5平移磁场源；全过程最小
  间隙9.83 mm，无碰撞、无自动复位、桌面接触率100%。
- 校准结果：胶囊沿世界+X移动97.79 mm，Y漂移10.75 mm，长轴滚角10.38 rad，
  滑移率0.310；全部自动判据PASS。
- 交付：`scripts/table_motion/test_06_composite_motion.py`、组合状态机及日志判据；
  结果见`logs/table_motion/composite_motion/20260731_004014/summary.json`。

## 2026-07-31 — 平桌磁控系统迁移至水平胃部任务

- 新增`scripts/stomach_motion/`独立入口，复用平桌有限场逆解、Ball轨迹和机械臂
  梯度控制；胃部版保留胃液角阻尼、重力、磁力和胃壁接触。
- 三个基本功能及连续组合均从胃部下端
  `(1.0608155, 0.1145374, 0.0065)m`启动；胶囊仍为被动刚体。
- 模式1实测方位变化76.67°、最终轴误差5.79°、接触率100%，全部判据PASS。
- 组合姿态链完成：侧躺88.15°→直立2.84°→29.40°倾斜旋转361.25°→
  直立2.55°→侧躺83.09°；无ASM碰撞或提前终止。
- 胃壁滚动仅沿+X移动11.29 mm，未达到平桌100 mm；按当前“只迁移、不补偿胃部
  误差”范围保留真实FAIL，不放宽定量判据。详见
  `docs/STOMACH_MOTION_MIGRATION.md`。

## 2026-07-31 — 修复胃部胶囊相机黑屏

- 诊断确认原始与圆形RGB均值均为0，问题来自相机近壁安装和照明，不是预览窗口。
- 将相机移至胶囊局部-Z端并旋转光轴朝外；近裁剪面保持0.5 mm。
- 将四颗LED移至镜头后约1 mm并把强度由40提升至250，避免灯落在仅约1.45 mm远的胃壁后方。
- `zero_agent.py`新增相机光轴、LED位置、图像落盘及`--capsule_side_pose`诊断。
- 侧躺复测RGB均值由0.000升至0.343，720p圆形画面恢复；30 Hz调试预览与1 Hz训练记录频率保持分离。

## 2026-07-31 — 胶囊相机组织渲染与外部姿态视图

- 定位灰白画面为两层遮挡：无材质碰撞网格覆盖可视网格，且相机光心位于不透明胶囊端盖内1 mm。
- 保留胃壁PhysX碰撞但关闭其RTX可见性；光心移至-Z端面外0.2 mm，近裁剪面改为0.1 mm。
- 胃部任务穹顶光500→30、四LED 250→15，LED半径0.8→1.5 mm、色温5000 K；胃壁肉色纹理与柔和高光恢复。
- 最终复测有效画面平均RGB约(0.753,0.562,0.496)，高光裁剪率0%，深度视距由1.45 mm恢复到约0.13 m。
- 新增`--capsule_pose_view`：960×540、30 Hz、世界坐标固定俯视偏移的胃腔内跟随窗口；可与30 Hz内镜预览同时启用。
- 外部视图已接入zero-agent、平桌/胃部运动测试和接触物理测试；工程视图不进入训练观测或数据集。

## 2026-07-31 — 区分旋转锥轴与瞬时角速度轴并校准至45°

- 回查上一轮完整胃部组合日志：世界+Z旋转轴与胶囊纵轴夹角中位29.40°，
  但瞬时角速度轴与胶囊纵轴夹角中位85.24°，确认目测接近90°并非显示误差。
- 将平桌/胃部的倾斜方位运动和组合运动目标统一由30°/55°改为45°，同步更新
  阶段名称、验收范围和请求指标。
- 新增`cone_deg`与`omega_axis_deg`遥测，分别记录几何锥角和瞬时角速度轴夹角，
  避免继续混淆两种“旋转轴”定义。
- 胃部模式1复测PASS：固定倾斜段几何锥角中位47.27°（46.28°–47.99°），
  方位变化78.63°、最终轴误差3.69°、接触率100%、无ASM碰撞。
- 瞬时角速度轴中位仍为82.21°；轴向充磁且磁矩与胶囊长轴同轴时，
  `m×B`不能直接产生绕长轴力矩。若目标是瞬时运动轴45°，需增加可实现的轴向
  自旋来源（非同轴/斜置磁体、内部转子，或经实物标定的接触复合运动），不能只改锥角参数。

## 2026-07-31 — 修正45°姿态极性并降低近壁过曝

- 明确安装关系：相机头位于胶囊局部`-Z`、磁轴为局部`+Z`；将请求定义为
  磁轴相对世界XY平面`-45°`，等价控制器极角135°，相机头目标仰角`+45°`。
- 组合运动改走头朝上半球：侧躺→头朝上直立→头朝上45°方位旋转→直立→侧躺；
  新增有符号`head_elev_deg`及朝上验收，禁止再以无符号轴误收头朝下解。
- 胃部模式1复测PASS：最终相机头仰角+33.95°、有向轴误差11.27°、方位变化
  77.96°、接触率100%、无ASM碰撞。
- 组合姿态回归通过：方位旋转356.19°、头部仰角中位+35.09°、无碰撞/离地；
  总结果仍因既有胃壁滚动仅11.17 mm（目标100 mm）而FAIL，与本轮姿态无关。
- 近壁照明调整：穹顶光30→15，四LED 15→8、半径1.5→2.0 mm、色温
  5000→4800 K；胃壁粗糙度0.50→0.62、IOR 1.50→1.38。
- 圆形有效图像复测：RGB均值约(0.682,0.455,0.385)，亮度P99=0.860，
  任一通道饱和比例0%；保留柔和湿润高光但消除数值裁剪。参数仍待真实DS01、
  LED光通量和湿组织BRDF标定，不能视为最终光度拟真。

## 2026-07-31 — 用户复核头朝上姿态

- 读取用户最新模式1日志：测试PASS，目标相机头仰角+45°，实测+33.95°，
  有向误差11.27°；方位变化77.96°、接触率100%、无ASM碰撞。
- 用户目测确认方向基本正确、仅有小偏差；保留当前开环物理响应，不用运动学
  瞬移或强制姿态掩盖胃壁接触、重力和有限磁矩造成的稳态误差。

## 2026-07-31 — 胃部环境物理引擎审计

- 确认动力学由NVIDIA PhysX以240 Hz积分：胶囊为5.735 g动态刚体，胃壁为
  静态双面精确三角碰撞网格，启用-Z重力、摩擦、零恢复系数及接触传感。
- 胃壁材料为静摩擦0.20、动摩擦0.15、恢复系数0；胶囊侧柔顺接触参数为
  25,000 N/m和300 N·s/m，并设置线/角阻尼、速度与穿透修正上限。
- 磁场不是PhysX内建电磁求解：Magpylib使用N52有限尺寸立方体/圆柱解析场及
  网格力矩积分，240 Hz更新后以外力/外力矩注入PhysX，并带0.15 s滤波和限幅。
- RTX负责内镜视觉而非力学；胃壁目前仍是刚性静态壳体，尚无FEM软组织、
  黏弹性、蠕动、胃液CFD/润滑或组织形变，因此当前为“刚体接触+解析磁力”
  基线，真实性依赖后续实物标定，不能由引擎本身保证。

## 2026-07-31 — GitHub源码留档与接触标定工具链

- 整理本地Git首个源码快照：纳入代码、配置、文档及轻量USDA层；排除10 MB
  胃部二进制USD、67 MB纹理、日志和数据集，避免把大资产混入源码仓库。
- 旧连接器账号曾初始化`zs020700/spherial-magnet-actuation`，但用户随后登录的
  GitHub CLI账号为`shuozhang1007`；最终归档改用该登录账号下的
  `robotarm-magnetic-lab`私有仓库，避免跨账号推送。
- 新增无侵入接触标定：临时USD层同时覆盖胶囊/胃壁摩擦，并扫描柔顺接触
  刚度和阻尼；基础场景参数不被试验修改。
- 五组敏感性扫描全部完成：三高度跌落和三区域滑动均PASS；高摩擦将部分
  区域滑移由24.75/6.32 mm降至14.81/1.73 mm，参数影响方向可观测。
- 当前结果只证明数值稳定和参数敏感性；新增实测目标模板及归一化RMSE拟合器，
  必须填入组织仿体跌落/滑移实测数据后才能称为物理标定。

## 2026-07-31 — GUI三高度无磁力跌落回归

- 在Kit主视窗及30 Hz胶囊外部跟随视图中运行5/15/30 mm胃壁跌落测试。
- 总结果PASS：三高度均接触并稳定贴壁，末段接触率100%，无穿透、异常反弹
  或持续抖动；最终速度分别约0.000199、0.000026和0.000017 m/s。
- 接触历史峰值分别为0.317、0.329和0.298 N；测试期间磁力门控输出为零。
- 结构化结果保存至`logs/contact_physics/drop/20260731_150710/summary.json`。

## 2026-08-03 — 模型训练就绪度审计

- 仿真、九轴动作接口、RGB-D/状态记录器、数据校验和时序索引已具备；现有数据
  仅1个12步样例、2个时序样本，只能验证管线，不能训练有效模型。
- 发现接口时序不一致：胃部策略相机实际1 Hz，协议仍声明30 Hz；30 Hz仅为
  工程预览，而策略20 Hz、现有8步动作块仅0.4 s，正式采集前必须升级协议。
- 正式训练尚缺任务成功定义/指标、胃部专家教师数据、初始状态与视觉/物理域
  随机化、训练/验证/测试划分、EndoVLA数据适配与训练入口及安全控制层。
- 当前奖励仅为存活/平滑/碰撞bring-up项，现有`scripted_tilt`不是专家策略；
  接触参数仍需组织仿体实测，真实相机、磁场及执行延迟仍需Sim-to-Real标定。

## 2026-08-03 — 统一1 Hz视觉与20 Hz控制时序协议

- 新增不可与v1混用的`robotarm_magnetic_policy 2.0.0`：物理240 Hz、低层控制
  20 Hz、胶囊相机/VLA推理1 Hz、工程预览30 Hz，动作块固定20步/1秒。
- 原始Episode仍按20 Hz记录状态和动作，但仅在真实新相机帧到达时保存一份
  RGB-D；其余控制行复用最近帧路径并记录帧ID、相机时间戳和新帧标志。
- 实测异步时钟新帧位于控制步0/20/40/61/82/103；协议保留真实时间戳并声明
  标称20步、允许±1步采样抖动，不伪造或重复相机帧。
- v2胃部样例采集120控制步、6个独立RGB-D帧；完整性验证PASS，生成2个
  4帧历史→20步动作块样本，历史帧均不重复。
- v1历史样例继续通过校验与8步旧索引，证明升级未破坏旧数据只读兼容。

## 2026-08-07 — 胃部组合运动可视化入口复核

- 确认最新入口仍为`scripts/stomach_motion/test_04_composite_motion.py`，默认使用
  胃部任务、Kit主视窗和实时节拍。
- 推荐同时启用1 Hz胶囊策略相机窗口与30 Hz外部姿态跟随窗口；30 Hz窗口仅用于
  工程观察，不改变v2训练协议或数据采集频率。

## 2026-08-07 — GitHub完整性与可执行性审计

- 本地`main`工作树干净且与`origin/main`同步，核心Isaac Lab代码、配置、
  脚本、文档及轻量USDA层已上传私有GitHub仓库。
- 当前仓库可在本工作站运行，但尚不是可在新机器上直接克隆运行的自包含包：
  胃部二进制USD/贴图、外部Stage/robotarm/asm资产和旧磁场扩展/XRDF未入库。
- 部分USD引用与Python配置仍使用`/home/multirobo`和`/mnt/isaac-linux`绝对路径；
  干净克隆需先做资产打包、依赖内聚、相对路径化及冷启动烟雾测试。

## 2026-08-07 — Isaac Lab胶囊运动验证边界复核

- 平桌场景已重复验证固定倾角方位旋转、直立/侧卧姿态转换和长轴被动滚动；
  长轴滚动稳定净位移约55.86 mm，组合运动实现约97.79 mm的+X位移。
- 胃部场景的倾角/方位控制已多次PASS；贴壁滚转转向重复得到12.18 mm位移和
  107.6°轴向改变。胃内组合运动的姿态段通过，但滚动段仅约11.2 mm，未达100 mm目标。
- 所有胶囊运动均为预设磁场/关节轨迹驱动的开环自然响应；胶囊位姿只用于
  日志和事后验收，不参与在线磁场、Ball或机械臂命令修正。

## 2026-08-07 — 动作原语暂停与切换能力审计

- 现有原语由`run_phase()`以20 Hz固定步数同步阻塞执行，组合运动的阶段顺序也是
  硬编码；尚无暂停时钟、取消令牌、抢占切换或安全过渡状态机。
- `terminated/truncated`只被记录而不立即中止当前阶段；Kit时间线暂停和
  强制中断不等于可恢复的安全暂停，运行中直接启动另一原语也不受支持。
- 当前若要保持已验收轨迹的正确性，应让单个原语完整执行；架构上可通过
  非阻塞`step()`、当前目标保持、五次过渡及安全监督器升级为可抢占原语系统。

## 2026-08-07 — 原子动作层新规格与第一阶段计划

- 完整审阅新规格：新协议用11个离散短动作替代旧9维关节动作，高层1 Hz、
  低层20 Hz、物理240 Hz，胶囊响应保持开环，部署执行器禁止读取胶囊真值。
- 第一阶段仅计划实现动作类型/结果/失败码、命令磁场与外磁体增量、
  `PRECHECK→PLAN→RAMP→MOVE→SETTLE→DONE/FAIL`状态机、动作掩码和时间戳。
- 新建独立原子动作Isaac Lab任务与1维离散动作适配层，保留旧任务作回归基线；
  磁场逆解改用配置的工作区参考点，不得由实时胶囊位姿构造。
- 验收将覆盖11动作终止性、同动作连续提交、掩码、关节/工作区/ASM路径
  安全、跟踪超时、单调时间戳和执行器特权信息隔离；不接VLM/RL，不验证胶囊短动作效果。

## 2026-08-07 — 安全判定与胶囊自然偏差解耦

- 第一阶段将“执行器安全完成”与“胶囊物理响应有效”分为独立结果；胶囊
  位移不足、姿态偏差、滑移或被胃褶皱阻挡不会导致执行器`FAIL`。
- 硬中止仅面向可部署且确定的风险：非有限状态、硬关节越界、实际冲突、
  严重跟踪失败和硬超时；胶囊真值、接触和胃部网格不进入硬安全条件。
- 关节软边界、工作区边缘和ASM预警间隙采用绿/黄/红分级、滞回与短时滤波；
  黄区只屏蔽继续靠近危险边界的动作，保留`HOLD/RETREAT`，不立即宣告失败。
- 第二阶段才用胶囊真值形成响应方向、有效位移和接触保持等验收/训练标签，
  不改变可部署动作状态机的`DONE/FAIL`语义。

## 2026-08-08 — 动作层修订规格与执行方案

- 完整审阅修订规格并与8月7日版差异核对：新版取消动作效果分级和软风险
  状态，执行器只返回`DONE`或`HARD_FAILURE`，胶囊响应偏差只进入奖励/Critic/离线评价。
- 状态机改为`IDLE→PRECHECK→PLAN→EXECUTE→DONE`；任一设备级硬安全失败进入
  `HARD_FAILURE→SAFE_RECOVER`，轨迹加减速和稳定保持仅作为`EXECUTE`内部子阶段。
- 执行方案分为规格冻结、纯命令核心、Isaac Lab ActionTerm与单环境接入、
  11短动作物理量测、SAFE_RECOVER独立验证、1 Hz动作边界/SMDP接入和Actor–Critic隔离七个里程碑。
- 在未验证撤离轨迹前，`SAFE_RECOVER`只能中止Actor控制、保持设备目标并终止回合，
  不得把未验证的自动撤离宣称为安全恢复。

## 2026-08-08 — 原子动作层第一阶段实现与验收

- 实现11个冻结动作ID、累积磁场/外磁体命令、动作掩码、整轨迹关节/工作区/XRDF
  预检及不可抢占状态机；结果严格限定为`DONE/HARD_FAILURE`，执行接口不含胶囊真值。
- 新增独立`Template-Robotarm-Magnetic-Atomic-Table-Lab-v0`任务：标量动作接口，20 Hz
  执行，240 Hz磁力更新；策略观测仅含九轴位置/速度和外磁体位姿。
- 修复旧表面任务初始化污染新动作配置、未标定瞬时加速度造成的误停及`TURN±`旧字段
  引用；计划加速度仍执行硬限制，实测加速度待标定滤波后再纳入硬停。
- 纯协议测试10/10通过；Isaac Lab中11/11动作均到达`DONE`，耗时0.95--1.05 s，
  最小ASM间隙约9.94--10.00 mm，未检测到碰撞。
- 原有9维平桌任务完成5步零动作烟雾回归，动作/观测/磁力桥均正常，新增任务未破坏
  既有验证环境。
- 新增原子硬失败termination term：保持最后安全目标后显式终止回合，避免执行器无限
  停留在`SAFE_RECOVER`；自动撤离仍未启用。

## 2026-08-08 — 原子动作层第二阶段实施方案

- 下一阶段定位为“1 Hz SMDP边界与短动作效果量测”，暂不训练Actor，也不把胶囊真值
  用于在线闭环或设备硬失败判定。
- 计划建立Actor transition与特权Critic/评价双通道，按请求ID关联但通过字段白名单
  隔离；随后在平桌多初态/多种子量测11动作的连续响应分布。
- 验收覆盖新RGB动作边界、不可抢占/不重复执行、连续效果指标、动作参数版本化、硬失败
  containment故障注入及旧任务回归；通过后再迁移胃部并接Actor–Critic训练。

## 2026-08-09 — TASK-001 胃部手动遥操作与覆盖评估

- 严格按Windows合同提交实现：冻结11动作键盘边界、独立胃部标量任务、1 Hz唯一帧覆盖、
  50 mm/120°圆形FOV、CUDA首次命中遮挡、累计掩码、记录和隔离3D/2D可视化。
- 验证：新增测试26/26、原阶段一10/10、表面11/11动作DONE、旧9维5步回归均通过；
  最终100帧覆盖耗时中位3.36 ms、p95 6.90 ms、最大13.25 ms，全部小于1 s。
- 修正性能采样反复HOLD造成的目标漂移，改为保持末次目标且不产生额外动作请求；执行器
  与动作语义未修改。完整结果见`handoffs/reports/TASK-001-p0-coverage-teleop-report.md`。
- 下一步：Windows端审查`feature/TASK-001-p0-coverage-teleop`及外部证据，未授权合并。

## 2026-08-09 — TASK-001 人工可视化验收说明

- 给出长时间键盘验收启动命令、11个冻结动作键位、边界重置/快照/退出键及状态含义。
- 操作要求：看到`P0_RESULT ... status=DONE`后再提交下一动作；忙碌期间按键会被丢弃，
  不排队。观察主视口运动与独立覆盖窗口红点逐步变绿，退出后核对会话证据目录。
- 本轮仅补充复现说明，不修改仿真、动作、覆盖或安全逻辑。

## 2026-08-09 — TASK-001 覆盖率实时视图修复

- 用户验收时未见绿色覆盖；最近会话证据表明计算正常（重置前12.43%，退出时2.61%），
  问题是覆盖点与胃壁同处主USD舞台导致遮挡/深度冲突，且窗口无显式百分比。
- 将红绿点云、胶囊标记、轨迹和相机迁入独立USD上下文，点径增至1.2 mm，增加实时
  `Coverage: percent (covered/total)` HUD与1 Hz `P0_COVERAGE`终端日志；不改物理、动作、
  光线判定、累计掩码或训练观测。
- 验证：覆盖/键盘测试24/24；GPU胃部集成11/11动作DONE、12个唯一覆盖帧、最终5.569%；
  GUI短时创建/退出码0，修复后的退出无视口异步清理异常。实现提交`1423fcd`。

## 2026-08-09 — TASK-001 机械臂/ASM穿透胃壁修复

- 定位到原11动作虽调用硬安全守卫，但只检查关节/工作区、ASM自碰撞和可选平面；
  旧`collision`终止也只表示ASM与机械臂自碰撞，胃壁网格未进入规划或实时判定。
- 胃部原子任务现用XRDF稠密`world_collision`球，对真实胃壁碰撞三角网格执行21点
  无符号扫掠距离预检，并在20 Hz执行期复查；5 mm缓冲内保持末次安全目标并报告
  `ENVIRONMENT_COLLISION`。只检查机械臂/ASM，胶囊—胃壁正常接触不受影响。
- 回归：纯安全测试16/16；胃部初始间隙53.741 mm；11/11动作独立运行均`DONE`。
  连续`APPROACH`在第5次、约4.639 mm时被拦截，制动后最小4.274 mm，几何间隙
  仍大于零，专项测试`STOMACH_GUARD_VALIDATION status=PASS`。

## 2026-08-09 — TASK-001 覆盖轨迹线宽警告诊断

- RTX/Hydra警告来自独立覆盖窗口的`BasisCurves`轨迹：代码只写入一个统一线宽值，
  但未显式把`widths`插值声明为`constant`，渲染器按逐顶点数据检查时发现数量不足。
- Hydra会尝试退化为常量线宽，因此只可能造成蓝色轨迹偶发不可见或闪烁；不影响物理、
  11动作、胃壁碰撞守卫、覆盖率计算或训练数据。建议后续显式设置常量线宽插值。

## 2026-08-09 — TASK-001 覆盖图自动保存核验

- `F12`仅用于运行中的人工快照；遥操作脚本在`Esc`、窗口正常关闭、脚本自然结束和
  已捕获的失败退出时均从`finally`调用`finalize("exit")`，自动生成最终覆盖投影PNG。
- 实际会话已核验存在`snapshot_*_exit.png`、`coverage_mask.npy`、
  `trajectory_world_m.npy`及`summary.json`；仅进程被强杀或系统崩溃时无法保证最终写盘。
- 现有功能已满足退出自动保存要求，本轮未重复修改仿真或覆盖实现。

## 2026-08-09 — TASK-001 目标80%自动覆盖运行

- 在原遥操作入口增加目标覆盖自动模式、动作预算/周期参数及机器可读统计；达到目标会自动
  退出并保存最终PNG、掩码、轨迹和报告，硬失败保持制动并记录准确失败码。
- 修复Ball原语在边界固化机械臂重力下垂的问题；六轴现持续保持上一次安全命令。动作层/
  覆盖测试40/40、胃部11动作11/11 DONE。
- 修复后5轮共运行387.191 s，接受116次动作，成功114、失败2、拒绝0；最高覆盖
  13.506%，未达80%。局部覆盖已平台化，继续盲循环会遇到磁场逆解、跟踪或安全边界；
  需先实现保持磁体—胶囊相对位姿的厘米级胃内导航。完整报告见
  `handoffs/reports/TASK-001-p0-coverage-80-run-report.md`。

## 2026-08-09 — TASK-001 大位移与安全边界审计

- 源码与5轮有效运行共同证明动作掩码未普遍封锁运动：116次请求全部接受，114次
  `DONE`、2次硬失败、0次`ACTION_MASKED`；11动作独立回归仍为11/11 `DONE`。
- 入口掩码只限制忙碌/断连/非有限状态/回合终止、倾角5--175°、方位奇异区、滚动与
  转向倾角65--115°及外磁体工作区；本轮没有请求因这些条件被拒绝。
- 规划与执行硬安全包括0.25°关节裕量、1.05倍速度、1.10倍计划加速度、XRDF自碰撞、
  真实胃壁21点路径扫掠和20 Hz实时5 mm胃壁间隙，以及0.015 rad/0.05 rad/s连续2步
  稳定判定和2.0 s执行超时。实际仅出现1次规划需6.665 s超过1.5 s上限、1次末端未
  稳定超时；5 mm守卫在不安全累计路径中触发过，但安全长运行间隙约9.996 mm。
- 大位移不足的主因是开环物理响应：`ROLL`每次仅平移外磁体7.5 mm，胶囊不与磁体
  运动学绑定且没有位置反馈重居中；胶囊磁力被限幅为5 mN、磁矩0.9 mN·m，并受胃壁
  摩擦（0.20/0.15）、接触和角阻尼影响。外磁体累计移动可达厘米级，胶囊仍主要产生
  毫米级位移和姿态变化，随后覆盖在局部视野内平台化。
- 结论：不应整体放宽安全边界。下一步应先量测每个动作的磁体—胶囊距离、原始/限幅
  磁力磁矩、接触力、滑移和净位移，再校准磁力/摩擦并加入动作边界级导航调度；仅对有
  数据支持的1.5 s规划上限和稳定超时做定向调整。

## 2026-08-09 — TASK-001 自动覆盖停止原因核验

- 最近5轮有效运行中，3轮分别在10或30次动作预算耗尽后以
  `max_action_calls_reached`正常退出；覆盖率平台化不会单独触发停止。
- 单向`ROLL_POS`轮前17次成功，第18次在2.05 s仍未满足0.015 rad位置误差和
  0.05 rad/s速度稳定条件，触发`EXECUTION_TIMEOUT`并安全保持末次目标。
- “10次ROLL＋连续AZIMUTH”轮前27次成功，第28次`AZIMUTH_POS`在磁场逆解附近需要
  6.665 s，超过1.5 s单原语上限，触发`PLANNING_FAILED`，不是碰撞。
- 更早的预修复轮在第7次`TILT_NEG`检测到l6球到胃壁仅3.659 mm，小于5 mm缓冲，
  触发`ENVIRONMENT_COLLISION`；该轮机械臂命令下垂固化问题随后已修复。

## 2026-08-09 — TASK-001 长时放宽验证方案（待确认）

- 计划把自动覆盖预算改为“覆盖达到80%、接受动作达到200次或墙钟达到600 s，三者任一
  先满足即在动作边界保存并退出”；另记录仿真时间，硬失败仍立即安全保持并退出。
- 建议把单原语最大规划时间从1.5 s增至2.5 s、执行截止从2.0 s增至3.0 s，稳定阈值从
  0.015 rad/0.05 rad·s⁻¹放宽到0.020 rad/0.080 rad·s⁻¹，连续2步要求不变。
- 建议胃壁缓冲仅由5 mm降至4 mm；真实零间隙仍禁止。21点路径扫掠、20 Hz实时复查、
  XRDF自碰撞、关节限位、速度/加速度和工作区保护保持不变。
- 先执行11动作回归和胃壁守卫专项，再做最长10分钟/200动作正式运行；输出停止原因、
  动作成功/失败/拒绝数、覆盖率曲线、胶囊净位移/活动范围及各项最小安全裕量。

## 2026-08-09 — TASK-001 长时放宽三轮验证

- 已实现200动作/600 s双预算；规划上限放宽至2.5 s、执行截止3.0 s、稳定阈值放宽至
  0.020 rad和0.080 rad/s，胃壁预测缓冲由5 mm降至4 mm，其余硬安全检查保持不变。
- 前置验证通过：逻辑测试41/41、GPU 11动作11/11 DONE、胃壁守卫专项PASS。
- 按要求恰好完成3次正式运行：合计活动墙钟1806.314 s、549次动作全部成功、0失败、
  0拒绝，最小ASM间隙9.993 mm；三轮均由600 s墙钟预算在动作边界正常结束。
- 三轮覆盖率均停在9.173%，胶囊净位移9.330--10.051 mm，活动范围约
  `7.091 × 9.417 × 0.916 mm`。放宽参数消除了误停，但确定性局部往复仍导致覆盖平台；
  后续应新增磁体重居中与跨区域导航调度，而非继续放宽碰撞保护。完整结果见
  `handoffs/reports/TASK-001-p0-extended-three-run-report.md`。

## 2026-08-15 — GitHub 双端连接复核

- Linux执行端确认`origin`为`xy211699-arch/robotarm-magnetic-lab`且具备读取和推送权限；
  `git ls-remote`成功，dry-run推送返回`Everything up-to-date`。
- `upstream`仍指向`shuozhang1007/robotarm-magnetic-lab`，仅允许读取，push URL保持
  `DISABLED`；工作区检查时无未提交修改。

## 2026-08-15 — TASK-002 理想胃壁表面控制器

- 从规划提交`98598b3`的隔离分支实现`ideal_surface_v1`：15个确定性标量动作、240 Hz
  五次轨迹、胃壁三角网格投影/邻接、胶囊接触分类、1 Hz RGB与既有遮挡覆盖评估；
  Actor仅接收RGB，不启用磁力或磁碰撞桥。
- 强制预检确认胶囊为直径13 mm、总长25 mm的Z轴胶囊体，相机光轴为胶囊局部-Z，
  胃壁网格24529顶点/49047三角形且向内法线符号为-1，并验证wxyz根位姿/速度写接口。
- 1000个随机有效动作加75个定向动作全部通过：1075次请求/1075个唯一结果、15个ID
  全覆盖、无非邻接跳变/硬穿透/异常硬失败；636个唯一1 Hz覆盖帧，最终覆盖12.145%。
- 回归通过：专项46/46、既有覆盖与动作层41/41、GPU覆盖几何PASS、胃部和桌面原子动作
  均11/11 DONE、旧9D桌面任务5步可视化冒烟PASS；外部证据与哈希见TASK-002报告。

## 2026-08-15 — TASK-002 键盘控制与可视化交付说明

- 确认交互入口为`teleop_ideal_surface_stomach_coverage.py`，按键适配器为
  `ideal_surface_keyboard.py`；Kit主视图与独立累计覆盖窗口随同启动。
- 提供15动作按键、复位/快照/退出操作及直接启动命令；策略相机和覆盖采样保持1 Hz，
  覆盖窗口最多30 Hz刷新，正常退出自动保存最终覆盖图和证据文件。

## 2026-08-16 — TASK-003 动态胶囊六方向受力遥操作

- 从规划头`027c558`隔离实现真实动态胶囊任务：240 Hz CPU PhysX、60 Hz环境/渲染、
  30 Hz相机；W/S、A/D、Q/E施加世界系COM力，默认0.5mg，命令力矩恒零，无运行期位姿修正。
- 预检PASS：胶囊质量5.735 g、直径13 mm/总长25 mm，重力与双层CCD开启；胃壁静态网格
  24529顶点/49047三角形、21条边界，接触传感器有效；专项47/47、既有回归87/87及四项
  live回归全部通过，120帧Kit冒烟正常退出。
- 六方向力均为0.02813017 N且误差低于1e-9 N，接触和动态位移明确可见；但现有冻结物理
  出现最小净空-1.982 mm及-Y单步1.0656 mm略超1.0517 mm边界，故按合同标记`partial`，
  未通过调参或位姿投影掩盖问题；完整证据见TASK-003报告。

## 2026-08-17 — 操作文档语言约定

- 后续新增或更新的操作文档默认使用中文；命令、代码标识符及必要的英文API名称保持原样。

## 2026-08-17 — TASK-003 可视化入口

- 确认交互入口为`teleop_dynamic_force_stomach.py --viz kit`；W/S、A/D、Q/E分别施加
  世界坐标系±X、±Y、±Z方向力，默认力为胶囊实时重量的0.5倍。

## 2026-08-17 — TASK-003 受力比例调整

- 经人工验收，将水平X/Y单轴力由0.5mg提高到0.9mg；新增独立竖直比例并设为1.1mg，
  保证纯+Z指令大于胶囊自重（理论净向上力约0.1mg），不改变动态刚体、重力和接触。
- 交互与验证脚本新增`--vertical_force_weight_ratio`参数；会话和验证结果同步记录水平、
  竖直两个比例，默认值分别为0.9和1.1。
- TASK-003专项纯逻辑与配置回归49/49通过，涉及文件的Python编译检查通过；当前自动执行
  会话因宿主未暴露CUDA设备而无法进入Kit场景，需在本机图形会话补做实际+Z运动观察。

## 2026-08-17 — TASK-003 胃部翻转与侧躺复位

- 不修改共享胃部USD，仅在TASK-003把胃部绕其世界包围盒中心、世界Y轴旋转180°，交换
  世界Z上下表面并保持原XY包围范围与中心不变；绕Z轴不能实现上下表面交换。
- 几何采样选择胃部长轴世界Y最小端起四分之一处：该处翻转后下壁法向Z分量约0.974，
  明显比世界X左四分之一处平整；胶囊中心设为`(1.04643745, 0.05368121,
  0.00190115) m`。
- 胶囊局部Z长轴投影到局部切平面，初始侧躺、零速度，并保留约0.2 mm采样净空；力、
  碰撞、重力、CCD、相机及时间步不变。
- TASK-003专项回归50/50及Python编译检查通过；派生配置隔离检查确认共享基础胃部任务
  仍为原根位姿。当前自动会话无CUDA设备，Kit动态落稳效果留待本地图形会话验收。

## 2026-08-21 — TASK-008 六动作动态力控制器（partial）

- 从规划头`a232ac4`和固定基线`06b15ca`隔离实现六个1秒动态力宏动作；物理/环境/
  相机/Actor为240/60/30/1 Hz，端点力严格转换为等价COM力矩，运行期不写胶囊位姿或速度。
- 新增确定性平面标定与独立留出协议、相机朝向三角法线覆盖门控、胃部单按键动作和外部/
  RGB/覆盖率三视图；修正Isaac Lab `wxyz`与SciPy `xyzw`四元数顺序。
- 全仓库回归163/163、旧动作层纯验证18/18通过；NVIDIA内核模块595.71.05与用户库
  595.84不一致，RTX/CUDA Warp无法初始化，故live前置检查、平面标定、冻结profile与
  胃部三视图未验收，按合同返回`partial`，未伪造候选或留出结果。详见TASK-008报告。

## 2026-08-22 — TASK-008 NVIDIA启动阻塞复核

- 再次执行`nvidia-smi`仍返回`Driver/library version mismatch`：当前内核模块为
  `595.71.05`，用户态NVML为`595.84`。
- Isaac Lab并非所有功能都无法启动：CPU PhysX可进入场景初始化，但RTX相机、CUDA Warp
  与Kit可视化设备无法建立，导致TASK-008 live前置检查、标定和三视图启动卡住或退出。

## 2026-08-22 — TASK-008 重启后完整动态标定

- 重启后RTX 5090、NVIDIA 595.84及CUDA恢复；修复TASK-008宏动作遗漏的胶囊body CCD
  启用逻辑，live预检、CUDA覆盖几何、全仓库回归163/163及编译检查通过。
- 完成五个非HOLD动作各20个标定状态和20个独立留出状态的构建；静置接触力约0.05626 N，
  所有候选0 FAULT。MOVE在0.9mg下双向20/20通过。
- VIEW在3.0mg上限为15/20和16/20，UP最佳仅6/20，均未满足每动作至少16/20；因此
  未生成完整冻结profile、未消费留出集，也未进入胃部验收。当前TASK-008仍为partial，
  后续需由方案端授权修改动作方向/时序/判据或扩大参数模型，不能把失败profile用于胃部。

## 2026-08-22 — TASK-008 桌面可视化诊断入口

- 新增`teleop_table.py`，在平整桌面中提供外部视口、胶囊相机窗口和动作状态窗口；单次按键
  严格执行一个1秒/240子步动态力宏动作，并输出MOVE位移、VIEW有符号倾角或UP抬升角。
- 默认使用实测诊断候选MOVE=0.9mg、VIEW=3.0mg、UP=2.197265625mg；结果逐动作保存到
  `/tmp/task008-table-visual-inspection`。该入口用于观察失败原因，不代表冻结profile或胃部验收。
- 专项回归23/23、Python编译和六动作RTX无窗口冒烟通过；单次顺序测试中HOLD、双向MOVE、
  双向VIEW通过，UP抬升角为-64.806°并失败，直观复现UP方向/响应问题。

## 2026-08-22 — TASK-008 三组力度命令行接口方案（待确认）

- 计划保留MOVE、VIEW、UP三个独立`mg`倍率参数，在启动可视化时直接指定，不改变1秒动作
  时序、施力端点、方向、动态刚体、重力或碰撞设置；MOVE倍率表示两端点合力，VIEW/UP
  表示相机侧端点力。
- 启动时和状态窗同时显示倍率及按实时质量换算的牛顿值，输出JSON记录相同配置；参数执行
  前校验有限、正值且不超过授权上限3.0，确认后补充帮助文字与回归测试。

## 2026-08-22 — TASK-008 三组力度接口实施

- 用户可视确认UP方向正确、仅力度过大；更正此前把连续六动作末端负仰角解释为方向问题的
  推断，该指标受前序动作累积状态影响，不能代表独立UP方向。
- 完善`--move_force_ratio`、`--view_force_ratio`、`--up_force_ratio`三个独立CLI参数：
  校验范围为`(0,3.0]`，帮助文字明确MOVE为双端总力、VIEW/UP为相机侧端点力。
- 启动日志、状态窗、逐动作JSONL、汇总JSON及独立`force_configuration.json`同步记录实时
  质量、自重、倍率和牛顿值；控制时序、施力点、方向、动态物理与碰撞均不变。
- Python编译、专项回归24/24及真实RTX/PhysX自定义参数HOLD冒烟通过；以0.20/0.50/1.05
  启动时解析为MOVE总力0.011252 N（每端0.005626 N）、VIEW 0.028130 N、UP 0.059073 N。

## 2026-08-22 — TASK-008 MOVE低力度失败核验

- 用户命令显式把MOVE从既有通过值0.9mg降至0.20mg；接口传值和公式核验无误，不是代码
  回归。复位初态单动作实测0.20mg位移4.846 mm，低于固定5 mm门槛而FAIL；0.9mg对照
  位移153.439 mm并PASS。
- 补测0.25mg对应总力0.014065 N、每端0.007033 N，复位初态位移6.097 mm并PASS；该单点
  可作为较小力度的手动观察起点，但不替代20样本正式标定。

## 2026-08-22 — TASK-008 驱动链路与机械臂运动核验

- TASK-008动作配置已覆盖基础任务的关节位置和磁场动作，仅保留`dynamic_force_macro`；控制项
  将端点力换算为等效COM力/力矩，并直接写入`capsule`动态刚体，不调用磁场模型或机械臂关节。
- 桌面任务仍继承包含robotarm/ASM的完整场景，并将机械臂注册为受重力、接触和隐式关节驱动
  共同作用的动态Articulation；每个宏动作推进240个物理子步，因此机械臂可能独立落稳或振动。
- 结论：胶囊驱动方式符合“直接施力”，但TASK-008桌面场景尚未做到机械臂视觉/动力学隔离；
  后续若需无歧义验收，建议从该专用场景移除robotarm/ASM，而不是把其运动解释为磁力传递。

## 2026-08-22 — TASK-008 确认力度迁移至翻转胃部

- 将桌面人工确认倍率MOVE=0.40mg、VIEW=0.30mg、UP=0.80mg设为TASK-008胃部任务默认值，
  并在胃部入口暴露同名CLI参数；仍采用直接刚体外力，不接入磁场模型。
- 胃部宏任务继续继承TASK-003专用场景变换：胃部绕世界Y轴180°翻转，胶囊在Y-min端起
  四分之一处以侧躺姿态复位；共享胃部USD及其他任务配置未修改。
- 胃部入口显式创建并立即刷新独立`P0 Stomach Coverage`红/绿覆盖点云窗口，空闲期间按
  30 Hz维持显示；外部视口、胶囊相机视口和覆盖率视口三者并存。
- 按用户要求未运行物理调试；Python编译检查和Isaac Lab配置/视图专项测试7/7通过。

## 2026-08-22 — TASK-008 胃部力度微调与提示精简

- 胃部默认倍率调整为MOVE=0.40mg、VIEW=0.25mg、UP=0.85mg；桌面任务、动作时序、施力
  位置、翻转胃部和四分之一处复位均保持不变。
- 删除动作状态窗口中的力度信息行，保留动作、阶段、仿真时间和覆盖率；终端结构化力度配置
  仍保留用于数据追溯。Python编译及专项配置/视图测试7/7通过，未运行物理调试。

## 2026-08-22 — TASK-008 UP端点约束、界面清理与低反光调整

- UP由“相机端单点向上力”改为姿态相关的等效纯力偶：相机端沿世界上方向的轴向正交投影
  受力，非相机端受等量反向力；合力为零且保留原抬升力矩，明确压低非相机端。相机恰好
  朝下时使用确定性横向脱离方向，恰好朝上时力偶归零；全程仍只写刚体外力/力矩。
- 胃部交互入口彻底删除中文动作/力度状态窗口及启动时力度配置打印，只保留外部视口、胶囊
  RGB和英文P0覆盖率窗口；结构化结果文件仍保存实际力度配置。
- 近距照明继续降高光：Dome 15→5、四LED 8→4、光源半径2→3 mm；胃壁材质粗糙度
  0.62→0.78、IOR 1.38→1.34，碰撞、摩擦和动力学未改。
- Python编译及TASK-008专项测试31/31通过；执行容器未暴露NVIDIA驱动/CUDA，真实RTX胃部
  UP冒烟无法启动，未伪造动态或视觉验收结论，留待本地图形会话观察。

## 2026-08-22 — TASK-008 UP施力点根因修正

- 根据人工验收否决上一版纯力偶方案，UP恢复为单个世界向上点力，不向非相机端施力，也不
  额外提交控制力矩。
- 修正真正根因：运行时读取`capsule_camera.cfg.offset.pos`，在胶囊局部正负两个半球球心
  中选择距相机光心最近者作为相机端；不再将施力端仅写死为局部轴符号。
- UP通过PhysX运行时接口以`positions=相机端球心`、`torques=None`提交，使抬升力矩只由
  正确位置的偏心点力自然产生；遥测新增世界作用点，便于可视验收追踪。
- Python编译及TASK-008专项测试31/31通过；真实胃部UP冒烟因当前执行容器未加载NVIDIA
  驱动、无CUDA设备而在Isaac Lab启动阶段终止，尚未形成动态可视验收结论。

## 2026-08-22 — TASK-008 覆盖率停滞修复

- 检查最近会话证据确认相机帧未冻结：帧号1→384、每帧候选669--1629个；但384帧仅42帧
  通过法线门控，单帧最多2点，最终仅27/24529（0.110%），因此视觉上近似恒定。
- 根因为薄壳胃壁 authored winding 指向组织外侧，而胃腔相机需要既有标定的向内法线符号
  `-1`。TASK-008覆盖运行时现显式使用`camera_facing_normal_sign=-1`，保留120°圆形FOV、
  50 mm距离和首次命中遮挡，不以关闭可见性约束规避问题。
- 每帧记录新增`first_hit_visible_count`与`normal_facing_count`，用于区分遮挡失败和法线方向
  失败；其他任务默认法线符号仍为`+1`。
- 覆盖几何与TASK-008专项测试58/58通过；当前执行容器仍无NVIDIA驱动/CUDA，真实RTX覆盖
  增长需在本地图形会话进行最终可视验收。

## 2026-08-22 — TASK-008 胶囊迁移至胃部另一侧四分之一处

- 沿翻转胃部纵向世界Y范围，将TASK-008复位区域从靠近Y最小端的四分之一处迁移到靠近
  Y最大端的四分之一截面；不修改共享胃部资产和TASK-003基线。
- 未直接镜像旧坐标：在目标截面重新采样真实下壁，避开原X位置Z法线仅0.859的斜坡，选择
  法线Z分量0.9984的平整点；按13×25 mm胶囊支撑距离与0.2 mm净空计算中心为
  `(1.078678615084, 0.177197008592, 0.004293035807) m`，侧躺四元数保持不变。
- TASK-003与TASK-008配置隔离、Python编译和专项测试37/37通过；动态落稳与相机画面仍需
  在本地图形会话验收。

## 2026-08-22 — TASK-008 另一侧复位穿模修正

- 人工验收确认X/Y区域正确但胶囊穿模。复核发现此前只按中心三角面计算净空；对12 mm
  中心轴做201点扫描后，完整胶囊最差净空为-4.274 mm，穿透发生在+轴端的弯曲壁面。
- 保持已确认的X/Y和侧躺姿态不变，仅将世界Z提高7.5 mm至0.011793035807 m；重新扫描的
  最小完整胶囊净空为0.573 mm，避免初始互穿，后续由重力和接触自然落稳。
- Python编译及TASK-003/TASK-008配置与控制专项测试37/37通过；本地图形会话需确认落稳
  期间无穿模、异常弹跳或滚出目标四分之一区域。

## 2026-08-22 — TASK-008 扩展为十四档动作协议

- 保留既有动作ID 0--5不变，追加ID 6--13；动作空间现为1个HOLD、正负各三档MOVE、
  正负各三档VIEW及1个UP，共14档，旧日志与数据协议保持兼容。
- MOVE三档合力固定为0.40/0.50/0.60 mg，VIEW三档相机端力固定为0.25/0.35/0.45 mg，
  UP保持0.85 mg；所有档位仍沿用原方向定义、端点施力、48/144/48子步和1秒边界协议。
- 胃部与桌面入口支持ID/名称脚本输入及字母键：MOVE为D/A、L/J、O/U，VIEW为E/Q、
  K/H、I/Y，Space与W分别保持HOLD和UP；覆盖、碰撞、复位点、照明及相机均未改。
- Python编译通过，动态力与覆盖专项回归71/71通过；当前执行会话无法连接NVIDIA驱动，
  未运行RTX胃部十四档可视冒烟，需由本地图形会话完成最终人工观察。

## 2026-08-25 — TASK-009A 高频参数化控制器基线发布

- 从混合修改工作区安全隔离并发布240 Hz物理/10 Hz动作、六模式`[mode, alpha]`控制器；
  MOVE范围按用户确认冻结为0.70--1.20 mg，VIEW/UP保持0.30--0.90/0.70--1.00 mg。
- 57项自动化测试通过；11周期live序列全部为24子步，主动模式24/24施力、HOLD 0/24，
  RGB帧1→12，Actor观测仅`policy.rgb`且状态均有限。
- 控制器提交为`335c5f563da51c50656729db86a7872809c58ada`；原混合工作区未清理或覆盖，
  外部日志和轨迹仅在`/tmp`及Kit日志目录留证，未提交数据本体。

## 2026-08-26 — TASK-009B 胃部覆盖环境 Gate 1 与入口标定 Gate 2

- 从规划提交`c57ce69873cecd7c21db05f5e656bf4f77b4b626`建立隔离分支；完成单环境胃部
  与TASK-009A六模式参数化力控制器集成，固定240 Hz物理、10 Hz动作、每边界24子步和
  每边界一次RGB采集；六模式live序列通过，状态和图像均为有限值。
- 新增世界轴对齐胃入口包围盒标定工具：使用三角形/AABB分离轴相交测试，实时高亮候选
  面片并显示中心、尺寸、面片数、面积及共享边连通分量；保存配置绑定胃壁几何SHA-256和
  配置SHA-256，包围盒变化可使后续位姿库失效。
- 自动化回归19/19通过；当前工具执行容器无NVIDIA驱动和图形显示，无法代替本机现场
  标定。任务按合同停在Gate 2，等待用户运行可视化、保存并确认入口区域；未生成位姿库。
- 首次现场启动暴露Kit 110 USD绑定兼容问题：`Mesh`不提供显示Primvar插值便捷方法，且
  Hydra会读取属性逐项写入时的临时空点网格。现改用`UsdGeom.PrimvarsAPI`设置插值，并在
  拓扑前预置合法点缓冲，消除`AttributeError`及`invalid points buffer`根因。
- 用户随后以新合同完全取消三维包围盒：Gate 2改为Dynamic胶囊暂停物理WASD平移、零力
  自然下落、0.25秒稳定确认、真实最近三角面点和共享边Dijkstra测地区域。实现固定10/2
  mm/s移动、2秒稳定上限及10--30 mm五档半径，并保存相互哈希绑定的锚点与区域配置；
  TASK-009B控制器、位姿库数量、覆盖率和三视图要求未改，仍等待现场双确认。
- 首次现场WASD日志确认候选X/Y已改变而仿真时间保持0.0秒；随后发现Kit键盘后端会混合
  返回带`name`属性的Input对象和裸字符串。回调现兼容两种格式，避免特殊键触发
  `AttributeError`，不改变按键映射、移动速度或物理暂停语义。
- 用户完成30 mm入口区域现场保存：种子面片17589、1375面/752顶点、面积0.0017662011 m2
  且单连通。因该区域偏小，新增60 mm档及已确认锚点恢复入口；恢复时校验配置与胃壁哈希，
  重算面片、顶点、面积及区域哈希，不重复下落，也不直接修改派生JSON。等待用户观察60 mm
  高亮区域并按Enter重新确认。
- 用户现场否决基于旧锚点扩大的区域：局部高亮落在非目标胃壁。后续拟改为复用TASK-009B
  的10 Hz参数化MOVE/VIEW/UP动力学控制重新选择胶囊稳定位置，再以5 mm步进选择10--80 mm
  共享边测地区域；本轮仅形成方案，等待确认后实施，既有锚点和区域文件暂不改动。
- 用户确认实施：入口标定定位阶段改为复用240/10 Hz参数化力ActionManager，MOVE/VIEW/UP
  全程保持Dynamic和真实接触；Enter切换完整HOLD周期且不清零惯性，稳定仍按240 Hz子步检查
  0.25秒、2 mm/s和5 deg/s。测地半径扩为10--80 mm、5 mm步进；旧配置在新锚点确认前保留。
- 用户完成新入口现场验收：动力学锚点哈希`0166638c...b6fc`，种子面片34914；最终选择
  55 mm测地半径，区域含6171面/3213顶点、面积0.0063698492 m2且单连通，区域哈希
  `1c38444c...26e7`。本地复核两份JSON的schema、配置哈希、相互引用和胃壁几何哈希均通过；
  Gate 2标记为pass，Gate 3位姿库尚未启动。

## 2026-08-26 — TASK-009B Gate 3 有效初始位姿库

- 从用户确认的55 mm测地区域按三角形面积采样，随机化切向长轴、相机端方向和0--360度
  滚转；候选状态仅施加HOLD并最多松弛2秒，严格执行连续0.25秒低于2 mm/s、5 deg/s、
  相机位于胃腔及无方向长轴角不小于45度的冻结门限，未放宽任何判定。
- 生成1200条互异状态：train/validation/test为1000/100/100，实际尝试1031/101/105；
  拒绝30条未按时稳定、6条相机不在胃腔、1条长轴角不足。数据本体外置，大小1935122
  字节，SHA-256为`7a7a20e1...a855`，Git仅保存生成器、种子/分组清单和外部工件清单。
- 固定回载三组各20条，共60条：位置和四元数可恢复，RGB、位姿和速度均有限，长轴角全部
  有效；回载日志37279字节，SHA-256为`b88fd775...08d6b`。独立审计确认1200个ID、候选
  种子及位姿指纹全唯一，分组无交集且数据哈希一致；Gate 3标记为pass。

## 2026-08-26 — TASK-009B Gate 4 七十毫米面积加权覆盖

- 将旧50 mm顶点计数覆盖升级为冻结合同要求的70 mm面积覆盖：每个目标三角形面积的三分
  之一分配给三个顶点，并同时执行120度圆形FOV、胃腔侧法向和CUDA第一命中遮挡；累计率、
  结构化日志、快照一致性及HUD统一改用面积，不修改控制器、物理或位姿库。
- 非均匀面积、距离/视场边界、法向、遮挡、累计并集、reset清空和单调性等回归57/57通过。
  live从Gate 3固定20/20/20位姿加载，60个边界均有非零当前可见面积，累计面积比例单调增至
  53.4965%；reset后清零并重算`C0=1.9400%`，61个RGB和物理状态全部有限。
- 旧目标集保持24529顶点、49047面、总面积0.0644836229 m2，几何哈希仍为`17ae0bc...9c1b`；
  live日志36309字节，SHA-256为`2758caf3...b856`。0.1秒浮点调度有21个边界未自动标记相机
  outdated，验证器仅在这些边界执行不推进物理的必要传感器采集，并逐条记录该事实。

## 2026-08-26 — TASK-009B Gate 5 同时间线三视图待验收

- 新增胃部覆盖遥操作入口：同一仿真时间线显示60 Hz外部主视口、严格复用训练记录传感器的
  10 Hz圆形胶囊RGB和10 Hz隔离覆盖窗口；覆盖颜色固定为红色未覆盖、绿色历史累计、蓝色
  当前可见，HUD使用70 mm面积加权比例，未创建额外30 Hz相机。
- 自动回归59/59通过；无界面3周期HOLD/MOVE/VIEW烟雾测试中RGB帧1→4、每周期24子步、
  覆盖率13.375%→17.669%且状态有限；Kit 2周期测试确认三个窗口均可创建。
- 当前任务状态为`needs_input`：只剩用户按中文验收文档进行长期键盘操作并确认三个画面，
  人工确认前不把Gate 5或TASK-009B标记为完成。
- 核对数据落盘：Gate 3位姿库本体、生成摘要、接受种子和拒绝记录均位于外部工件目录，
  固定60条回载另有验证摘要/JSONL；三视图退出时会自动保存覆盖PNG、同名JSON、mask、
  轨迹和summary。用户最近一次现场会话退出覆盖率为39.813%，自动图片路径已确认存在。
- 按用户要求将原`robotarm_magnetic_lab_artifacts`整体迁移至主仓库`artifacts/`；旧目录已
  不存在，四类工件共29个目录/文件均可读取。新增Git-common-dir路径解析，确保从`/tmp`
  TASK工作树启动时仍写入主仓库；更新所有TASK-009B默认输出、清单路径和清单哈希，59项
  回归及路径/内容哈希审计通过。`artifacts/`保持Git忽略，目录规则另见工件存储文档。
- 本轮复核当前10 Hz参数化力合同：MOVE总合力0.70--1.20 mg并由两端均分，VIEW为相机端
  0.30--0.90 mg，UP为相机端世界+Z方向0.70--1.00 mg；`alpha`在上下限间线性插值，
  HOLD为零Actor力。该结论来自TASK-009A/009B实际ActionTerm配置，不引用旧1秒宏动作。
- 用户根据实测将现行范围改为MOVE 0.70--1.40 mg、VIEW 0.20--0.50 mg、UP
  0.80--1.05 mg；纯合同、ActionTerm和端点/中点冻结测试同步更新，时钟、方向、作用点及
  HOLD语义未变。旧范围记录保留为历史，后续主动响应数据以新范围报告和提交为准。
- 新范围自动回归59/59通过；胃部live六模式边界均为24子步，主动模式24/24、HOLD 0/24，
  RGB逐边界更新且状态有限，遥测中点MOVE/VIEW/UP分别为1.05/0.35/0.925 mg。结构化证据
  已保存到主仓库`artifacts/task009b_force_range_validation`并写入独立交接报告。
## 2026-08-26 TASK-009B补充验收：RGB同步、GPU执行与三视图

- 正式胃部任务取消CPU强制覆盖，注册GPU训练环境；`env.step()`策略观测在每个10 Hz边界
  保证恰好一帧新RGB，覆盖端校验并复用同一帧。
- reset内置10个HOLD周期（24子步/周期），返回Actor前清零回合预算；私有相机补采接口集中
  隔离并标注Isaac Lab升级风险。
- 自动验收通过：53项回归；cuda:0上1000个连续混合动作边界、100次reset/1000个HOLD帧；
  固定train/validation/test各20条、共60条位姿GPU回载全部通过。
- 保持面积加权覆盖、70 mm、24529顶点/49047面、入口区域、位姿库及MOVE/VIEW/UP冻结力度
  不变。三视图已启动，等待用户按Y/N给出人工结论，当前总状态`needs_input`。
## 2026-08-27 可达区域覆盖率方案讨论

- 用户发现部分胃壁区域对当前胶囊/控制系统不可达，提出以一个或多个人工种子点沿胃壁测地扩展，形成不可达区域掩码。
- 方案可行；建议在评估前冻结并哈希该掩码，覆盖率仍按面积权重计算：有效目标为原ROI减去不可达掩码。
- 后续应同时报告原始全胃壁覆盖率、可达ROI归一化覆盖率和排除面积比例，避免事后选择造成指标偏差；本轮未修改代码或合同。

## 2026-08-27 — TASK-009B 人工不可达区域掩膜

- 新增独立三维游标标定器：支持多个胃壁种子、每种子10--80 mm/5 mm档测地扩展、红色并集
  预览、增删/切换和冻结保存；配置绑定操作者、物理/解剖理由、胃壁与面积权重哈希。
- 覆盖运行时保留原完整ROI累积器，并为剔除后面片重新分配顶点面积权重；同步输出可达ROI
  覆盖率、原始覆盖率和排除面积比例。灰色显示排除区，未改70 mm可见距离或控制器。
- 三视图和GPU正式验证入口新增可选`--unreachable_region`；未指定时行为完全保持原合同。
  覆盖/测地/哈希/颜色回归48项通过；受控终端无X/可见GPU，Kit现场启动被运行环境阻断，
  尚待用户在本机图形会话选点并冻结首份配置。
